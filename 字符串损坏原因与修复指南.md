# 字符串损坏原因与修复指南

## 1. 现象
- Python/TS 文件中原本正常的中文字符串变成乱码（mojibake）。
- 部分字符串出现 `unterminated string literal`（字符串未闭合）语法错误。
- 常见触发点是批量替换后、或脚本重写文件后。

## 2. 这次最可能的原因
1. **编码不一致导致内容被重编码**
- 文件原始编码与脚本读写编码不一致（例如原文件不是 UTF-8，但被按 UTF-8 重写）。
- 使用 `Set-Content` 重写整文件时，如果未显式指定正确编码，容易把中文破坏。

2. **正则跨大段替换导致字符串边界被误改**
- 用 `Get-Content -Raw` + Regex 替换大段代码时，容易把引号、转义或多行字符串截断。
- 一旦少了一个引号，后续会连锁触发大量语法报错。

3. **中断时机不佳**
- 在批量修改过程中中断，可能让文件停留在“已部分替换但未校验”的状态。

## 3. 如何修复（建议顺序）
1. **先备份当前损坏文件**
- 例如把 `proxy.py` 复制成 `proxy.py.broken.bak`。

2. **优先从版本库恢复干净基线**
- 如果在 Git 管理下，先恢复该文件到最近正常版本，再重新做小步修改。
- 这比逐行救火更稳。

3. **重新应用改动时只做小块 patch**
- 避免整文件正则替换。
- 每次改动后立即执行语法检查：
  - Python: `python -m py_compile path/to/file.py`
  - 前端: `tsc --noEmit`

4. **强制统一编码**
- 项目统一为 `UTF-8`（建议带 BOM 或不带 BOM，团队统一即可）。
- 在脚本里显式指定编码读写，不要依赖默认值。

## 4. 预防措施
- 不对超大文件做“一次性大正则替换”。
- 对含中文/多行字符串的文件，优先使用精确 patch（最小上下文）。
- 每做 1~3 处修改就跑一次语法检查。
- 批量操作前先 `git diff` 和 `git stash`/分支隔离，确保可回滚。

## 5. 推荐的安全编辑习惯（PowerShell）
- 读文件时显式编码：
  - `Get-Content -Raw -Encoding UTF8`
- 写文件时显式编码并避免无意重写整文件：
  - `Set-Content -Encoding UTF8`
- 更推荐使用补丁工具做局部修改，而不是“整文件读取 -> 正则 -> 整文件写回”。

